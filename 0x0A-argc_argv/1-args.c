#include <stdio.h>
#include "main.h"

/**
 * main - print the number of arg passed to the
 * program
 * @argc: no. of args
 * @argv: array of args
 *
 * Return: 0 for success
 */
int main(int argc. char *arg[] __attribute__((unused)))
{
	printf("%d\n", argc - 1);
	return (0);
}

#include "main.h"
#include <stdio.h>

/**
 * _isupper - checks for uppercase character
 * @c:is the character
 * Return: 1 if c is uppercase
 * or 0 for success
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
